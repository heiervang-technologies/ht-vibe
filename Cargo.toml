[workspace]
members = ["vibe-renderer", "vibe", "vibe-audio", "vibe-web"]
# Skip vibe-web for native builds — it only compiles for wasm32 targets.
default-members = ["vibe-renderer", "vibe", "vibe-audio"]
resolver = "2"

[workspace.dependencies]
anyhow = "1"
wgpu = { version = "29.0", default-features = false, features = [
  # from the default features
  "gles",
  "parking_lot",
  "std",
  "vulkan",
  "wgsl",

  # extra features
  "serde",
  "glsl",
  "naga-ir",
] }

serde = { version = "1", features = ["derive"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
thiserror = "2"
cgmath = { version = "0.18", features = ["serde"] }
clap = { version = "4.6", features = ["derive"] }
vibe-audio = { path = "./vibe-audio" }
image = "0.25"
xdg = "3.0"
toml = "1.1"
tracing-indicatif = "0.3"
bytemuck = "1.25"
colored = "3"
winit = { version = "0.30", default-features = false, features = [
  "rwh_06",
  "wayland",
  "wayland-csd-adwaita",
  "wayland-dlopen",
] }

[profile.release-lto]
inherits = "release"
codegen-units = 1
lto = true

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
